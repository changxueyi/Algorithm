package JD;

import com.sun.deploy.util.StringUtils;

/**
 * @ClassName BiztypeBuilder
 * @Description TODO
 * @Author changxueyi
 * @Date 2020/9/8 23:07
 */
public class BiztypeBuilder {
  /*  private volatile static R2MCacheUtilService cacheService;

    public static R2MCacheUtilService getCacheService() {
        if (null == getCacheService()) {
            synchronized (BiztypeBuilder.class) {
                if (null == getCacheService()) {
                    cacheService = SpringCtxHelper.getBean(R2MCacheUtilServiceImpl.class);
                }
            }
        }
        return cacheService;
    }

    //根据parentBizType 生成bizType
    public static String buildBizTypeByParent(String parentBiztype, String biztypeTag) {
        if (StringUtils.isBlank(biztypeTag)) {
            biztypeTag = getBiztypeTag(parentBiztype);
        }
        String[] arr = parentBiztype.split("_");
        int level = arr.length + 1;
        String bizType = buiBiz
    }*/
}
